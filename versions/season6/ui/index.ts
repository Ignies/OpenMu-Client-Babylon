/**
 * Season 6 UI: every window the base game has. A version that lacks a window
 * exports a component of the same name that renders nothing
 * (see `versions/_template/ui`), so `worldPage` never branches on the version.
 */
export { MasterSkillsWindow } from '../../../src/ui/pages/worldPage/components/masterSkills';
