/**
 * TODO: UI overrides. Every export of `versions/season6/ui/index.ts` must
 * exist here under the same name. A window this version does not have is a
 * component that renders nothing (below); a window that *differs* is a
 * sibling file here that reuses the base components. Windows that merely
 * disappear are better gated by `gameVersion.features.*` at their consumer.
 */
export function MasterSkillsWindow(): null {
  return null;
}
